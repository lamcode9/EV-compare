# battery.mom — Claude entry

`AGENTS.md` in this folder is the single canonical, platform-agnostic agent brief — authoritative for every agent (Claude, Codex, Cursor, and any future tool). It is imported below so it loads automatically here. Don't add rules to this file; put them in `AGENTS.md`.

@AGENTS.md
